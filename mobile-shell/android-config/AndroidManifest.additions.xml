<!--
  Merge these into android/app/src/main/AndroidManifest.xml after `npx cap add android`.
  Do not replace the file Capacitor generates — add these blocks inside the
  existing <application> and <manifest> tags.
-->

<!-- Inside <manifest>, alongside the existing INTERNET permission: -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
<uses-permission android:name="android.permission.CAMERA" />
<uses-permission android:name="android.permission.USE_BIOMETRIC" />
<uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
<uses-permission android:name="android.permission.VIBRATE" /> <!-- native haptic feedback -->
<!-- DownloadManager needs this to write into the public Downloads folder on
     Android 9 (API 28) and below; Android 10+ (scoped storage) doesn't need
     it for that destination, so it's capped with maxSdkVersion. -->
<uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />

<!-- Inside <application>, force cleartext off and pin the security config: -->
<application
    android:usesCleartextTraffic="false"
    android:networkSecurityConfig="@xml/network_security_config"
    ...>

  <!-- Existing MainActivity entry, plus a deep-link intent filter for
       https://jedidamarketplace.com/* and the custom scheme
       jedidamarketplace://, both validated in MainActivity.java before
       anything is loaded (see MainActivity.additions.java). -->
  <activity android:name=".MainActivity" ...>
    <intent-filter android:autoVerify="true">
      <action android:name="android.intent.action.VIEW" />
      <category android:name="android.intent.category.DEFAULT" />
      <category android:name="android.intent.category.BROWSABLE" />
      <data android:scheme="https" android:host="jedidamarketplace.com" />
      <data android:scheme="https" android:host="*.jedidamarketplace.com" />
    </intent-filter>
    <intent-filter>
      <action android:name="android.intent.action.VIEW" />
      <category android:name="android.intent.category.DEFAULT" />
      <category android:name="android.intent.category.BROWSABLE" />
      <data android:scheme="jedidamarketplace" />
    </intent-filter>
  </activity>
</application>
