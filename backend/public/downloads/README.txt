This directory is populated by the CI pipeline in /ci/.github/workflows/build-shell.yml
on every tagged release. It is intentionally empty in source control — real
binaries live here only on the deployed server, not in git.
