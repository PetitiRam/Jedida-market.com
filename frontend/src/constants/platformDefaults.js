// See backend/src/constants/platformDefaults.js for the reasoning:
// this is only shown when a product/order genuinely has no currency
// recorded — not a currency that overrides anything actually stored.
export const DEFAULT_CURRENCY = 'USD';
